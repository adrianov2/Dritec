document.getElementById("cadastroForm").addEventListener("submit", function(event) {
    event.preventDefault();

    // Aqui você pode adicionar lógica para processar o formulário, como enviar os dados para o servidor.

    // Exemplo de exibição de mensagem após o cadastro.
    alert("Cadastro realizado com sucesso!");
});
